apiVersion: v1
kind: Service
metadata:
  name: vote
spec:
  type: NodePort
  selector:
    name: voting-app-pod 
    app: voting-app-proj 
  ports:
  - port: 8080
    targetPort: 80
    nodePort: 31000
