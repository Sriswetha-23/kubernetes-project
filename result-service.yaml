apiVersion: v1
kind: Service
metadata:
  name: result
spec:
  type: NodePort
  selector:
    name: result-app-pod
    app: voting-app-proj 
  ports:
  - port: 8081
    targetPort: 80
    nodePort: 31001
